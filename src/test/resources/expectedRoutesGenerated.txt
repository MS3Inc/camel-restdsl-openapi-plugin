		interceptFrom("rest*")
			.process(new OpenApi4jValidator("oas-petstore.yaml", contextPath));

		rest()
			.put("/pet")
				.id("put-pet")
				.consumes("application/json,application/xml,application/x-www-form-urlencoded")
				.produces("application/xml,application/json")
				.route()
				.setProperty("origRouteId", simple("${routeId}"))
				.to(direct("logger-helper-start"))
				.to(direct("put-pet").getUri())
				.to(direct("logger-helper-end"))
				.endRest()
			.post("/pet")
				.id("post-pet")
				.consumes("application/json,application/xml,application/x-www-form-urlencoded")
				.produces("application/xml,application/json")
				.route()
				.setProperty("origRouteId", simple("${routeId}"))
				.to(direct("logger-helper-start"))
				.to(direct("post-pet").getUri())
				.to(direct("logger-helper-end"))
				.endRest()
			.get("/pet/findByStatus")
				.id("get-pet-findByStatus")
				.produces("application/xml,application/json")
				.route()
				.setProperty("origRouteId", simple("${routeId}"))
				.to(direct("logger-helper-start"))
				.to(direct("get-pet-findByStatus").getUri())
				.to(direct("logger-helper-end"))
				.endRest()
			.get("/pet/findByTags")
				.id("get-pet-findByTags")
				.produces("application/xml,application/json")
				.route()
				.setProperty("origRouteId", simple("${routeId}"))
				.to(direct("logger-helper-start"))
				.to(direct("get-pet-findByTags").getUri())
				.to(direct("logger-helper-end"))
				.endRest()
			.get("/pet/{petId}")
				.id("get-pet-petId")
				.produces("application/xml,application/json")
				.route()
				.setProperty("origRouteId", simple("${routeId}"))
				.to(direct("logger-helper-start"))
				.to(direct("get-pet-petId").getUri())
				.to(direct("logger-helper-end"))
				.endRest()
			.post("/pet/{petId}")
				.id("post-pet-petId")
				.route()
				.setProperty("origRouteId", simple("${routeId}"))
				.to(direct("logger-helper-start"))
				.to(direct("post-pet-petId").getUri())
				.to(direct("logger-helper-end"))
				.endRest()
			.delete("/pet/{petId}")
				.id("delete-pet-petId")
				.route()
				.setProperty("origRouteId", simple("${routeId}"))
				.to(direct("logger-helper-start"))
				.to(direct("delete-pet-petId").getUri())
				.to(direct("logger-helper-end"))
				.endRest()
			.post("/pet/{petId}/uploadImage")
				.id("post-pet-petId-uploadImage")
				.consumes("application/octet-stream")
				.produces("application/json")
				.route()
				.setProperty("origRouteId", simple("${routeId}"))
				.to(direct("logger-helper-start"))
				.to(direct("post-pet-petId-uploadImage").getUri())
				.to(direct("logger-helper-end"))
				.endRest()
			.get("/store/inventory")
				.id("get-store-inventory")
				.produces("application/json")
				.route()
				.setProperty("origRouteId", simple("${routeId}"))
				.to(direct("logger-helper-start"))
				.to(direct("get-store-inventory").getUri())
				.to(direct("logger-helper-end"))
				.endRest()
			.post("/store/order")
				.id("post-store-order")
				.consumes("application/json,application/xml,application/x-www-form-urlencoded")
				.produces("application/json")
				.route()
				.setProperty("origRouteId", simple("${routeId}"))
				.to(direct("logger-helper-start"))
				.to(direct("post-store-order").getUri())
				.to(direct("logger-helper-end"))
				.endRest()
			.get("/store/order/{orderId}")
				.id("get-store-order-orderId")
				.produces("application/xml,application/json")
				.route()
				.setProperty("origRouteId", simple("${routeId}"))
				.to(direct("logger-helper-start"))
				.to(direct("get-store-order-orderId").getUri())
				.to(direct("logger-helper-end"))
				.endRest()
			.delete("/store/order/{orderId}")
				.id("delete-store-order-orderId")
				.route()
				.setProperty("origRouteId", simple("${routeId}"))
				.to(direct("logger-helper-start"))
				.to(direct("delete-store-order-orderId").getUri())
				.to(direct("logger-helper-end"))
				.endRest()
			.post("/user")
				.id("post-user")
				.consumes("application/json,application/xml,application/x-www-form-urlencoded")
				.produces("application/json,application/xml")
				.route()
				.setProperty("origRouteId", simple("${routeId}"))
				.to(direct("logger-helper-start"))
				.to(direct("post-user").getUri())
				.to(direct("logger-helper-end"))
				.endRest()
			.post("/user/createWithList")
				.id("post-user-createWithList")
				.consumes("application/json")
				.produces("application/xml,application/json")
				.route()
				.setProperty("origRouteId", simple("${routeId}"))
				.to(direct("logger-helper-start"))
				.to(direct("post-user-createWithList").getUri())
				.to(direct("logger-helper-end"))
				.endRest()
			.get("/user/login")
				.id("get-user-login")
				.produces("application/xml,application/json")
				.route()
				.setProperty("origRouteId", simple("${routeId}"))
				.to(direct("logger-helper-start"))
				.to(direct("get-user-login").getUri())
				.to(direct("logger-helper-end"))
				.endRest()
			.get("/user/logout")
				.id("get-user-logout")
				.route()
				.setProperty("origRouteId", simple("${routeId}"))
				.to(direct("logger-helper-start"))
				.to(direct("get-user-logout").getUri())
				.to(direct("logger-helper-end"))
				.endRest()
			.get("/user/{username}")
				.id("get-user-username")
				.produces("application/xml,application/json")
				.route()
				.setProperty("origRouteId", simple("${routeId}"))
				.to(direct("logger-helper-start"))
				.to(direct("get-user-username").getUri())
				.to(direct("logger-helper-end"))
				.endRest()
			.put("/user/{username}")
				.id("put-user-username")
				.consumes("application/json,application/xml,application/x-www-form-urlencoded")
				.route()
				.setProperty("origRouteId", simple("${routeId}"))
				.to(direct("logger-helper-start"))
				.to(direct("put-user-username").getUri())
				.to(direct("logger-helper-end"))
				.endRest()
			.delete("/user/{username}")
				.id("delete-user-username")
				.route()
				.setProperty("origRouteId", simple("${routeId}"))
				.to(direct("logger-helper-start"))
				.to(direct("delete-user-username").getUri())
				.to(direct("logger-helper-end"))
				.endRest()
		;